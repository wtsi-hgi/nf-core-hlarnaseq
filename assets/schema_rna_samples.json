{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/hlarnaseq/master/assets/schema_rna_samples.json",
    "title": "nf-core/hlarnaseq pipeline - params.rna_samples schema",
    "description": "Schema for the RNA samples manifest provided with params.rna_samples",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "rna_id": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "RNA sample ID must be provided and cannot contain spaces",
                "meta": ["id", "rna_id"]
            },
            "bam": {
                "type": "string",
                "format": "file-path",
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.(bam|BAM)$",
                "errorMessage": "RNA BAM path must be provided, cannot contain spaces and must have extension '.bam'"
            },
            "bai": {
                "type": "string",
                "format": "file-path",
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.(bai|BAI)$",
                "errorMessage": "RNA BAM index path must be provided, cannot contain spaces and must have extension '.bai'"
            },
            "unpaired_r1": {
                "type": "string",
                "format": "file-path",
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.f(ast)?q\\.gz$",
                "errorMessage": "Unpaired read 1 FASTQ must be provided, cannot contain spaces and must have extension '.fq.gz' or '.fastq.gz'"
            },
            "unpaired_r2": {
                "type": "string",
                "format": "file-path",
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.f(ast)?q\\.gz$",
                "errorMessage": "Unpaired read 2 FASTQ must be provided, cannot contain spaces and must have extension '.fq.gz' or '.fastq.gz'"
            }
        },
        "required": ["rna_id", "bam", "bai", "unpaired_r1", "unpaired_r2"]
    }
}
