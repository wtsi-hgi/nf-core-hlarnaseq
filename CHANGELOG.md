# nf-core/hlarnaseq: Changelog

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/)
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## v1.0.0dev - [date]

Initial release of nf-core/hlarnaseq, created with the [nf-core](https://nf-co.re/) template.

### `Added`

- Added required `--rna_samples` and `--hla_region` inputs and extraction of arcasHLA-ready paired MHC FASTQs with samtools.
- Added per-sample validatefastq pairing validation for extracted arcasHLA reads, with validation logs under `arcashla/validation/`.
- Added an `ARCASHLA_GENOTYPE` step that runs `arcasHLA genotype` on validated RNA reads (configurable via `--arcashla_genes`), writing per-sample results under `arcashla/genotype/`. Genotyping runs inside a dedicated, operator-prepared `arcas-hla` Conda environment.
- Added an `ARCASHLA_COMBINE` step that merges per-sample arcasHLA genotype JSON results into a single long-format `arcashla/arcasHLA_combined.csv`, using a new `bin/combine_arcashla_genotypes.R` script.
- Added a standalone `bin/call_hla_consensus.py` script (argparse CLI) that calls a consensus HLA allele per WGS individual by combining `ARCASHLA_COMBINE` RNA genotypes with `HLALA_COMBINE` WGS genotypes via an RNA/WGS sample key. Not yet wired into the Nextflow workflow.
- Added a `--sample_key` pipeline parameter (comma-separated `rnaseq_sample_id,wgs_sample_id` mapping) and a new `HLA_CONSENSUS` process that wires `bin/call_hla_consensus.py` into the Nextflow workflow, running automatically when `--rna_samples`, `--wgs_samples`, and `--sample_key` are all provided, with results written to `hla_consensus/`.
- Added optional `--rna_excluded_samples`/`--wgs_excluded_samples` parameters (plain-text sample ID lists) and `--hla_consensus_truncate_fields` (default `2`), passed through to `HLA_CONSENSUS`.
- Added a `bin/consensus_to_hlapm.py` script that converts `hla_consensus.rna_wgs_hla_consensus.tsv` into one per-individual, 2-column HLApm input TSV per WGS group, filtered by a new `--hlapm_allowed_loci` allow-list (default `A,B,C,DRB1,DQA1,DQB1,DPA1,DPB1,DOA,DOB,G,E,F`), and a `bin/hlapm_build_personalized_ref.R` script (ported from `artifacts/scripts/hlapm-personref.R`) that runs [HLApm](https://github.com/davenportlab/HLApm)'s `bulkRNA_build_personalized_HLA_ref()` on those per-individual TSVs.
- Added `HLAPM_PREPARE_INPUT` and `HLAPM_BUILD_REF` processes and a new `HLAPM` subworkflow that wire the above scripts into the Nextflow workflow, running automatically whenever `HLA_CONSENSUS` runs (i.e. whenever `--rna_samples`, `--wgs_samples`, and `--sample_key` are all provided). `HLAPM_BUILD_REF` invokes HLApm inside a dedicated, operator-prepared `hlapm` Conda environment via `conda run -n hlapm`. Results are written to `hlapm/input/<WGS_sample_ID>.tsv` and `hlapm/personalized_ref/out/<individual_ID>/<allele>.{fa,gtf}`.
- Added a new, now-mandatory (once `--rna_samples`, `--wgs_samples`, and `--sample_key` are all provided) `--hlapm_repo` pipeline parameter pointing to a local, pre-cloned HLApm checkout, enforced via a new `hlapmRepoExistsError()` hard-error check mirroring the existing `hlalaGraphDirExistsError()` precedent.
- Vendored the [nf-core/modules `STAR_GENOMEGENERATE`](https://github.com/nf-core/modules/tree/master/modules/nf-core/star/genomegenerate) module (pinned to STAR 2.7.11b, matching the version used by nf-core/rnaseq to build this pipeline's test data) and added a new `HLAPM_LIST_STAR_TARGETS` local module and `HLAPM_STAR_INDEX` subworkflow, wired in immediately after `HLAPM`. It discovers every allele FASTA/GTF pair actually present under `hlapm/personalized_ref/out/<individual_ID>/*.fa` (not from any input/consensus TSV), deduplicates them by a content hash (sha256 of FASTA+GTF, not by allele name, since HLApm does not explicitly guarantee identical output for the same named allele call across individuals), and builds one shared STAR genome index per distinct allele under `hlapm/star_index/<allele_key>/star/`. `hlapm/star_index_targets/unique_alleles.csv` and `sample_alleles.csv` record the dedup mapping. Unlike the rest of the pipeline, this step expects STAR from a container (`-profile singularity`/`docker`) or an auto-managed Conda environment, not an operator-prepared one.
- Vendored the [nf-core/modules `STAR_ALIGN`](https://github.com/nf-core/modules/tree/master/modules/nf-core/star/align) module (pinned to the same STAR 2.7.11b version used for indexing) and added a new `HLAPM_RESOLVE_SAMPLE_ALLELES` local module plus `HLAPM_STAR_ALIGN` subworkflow, wired in immediately after `HLAPM_STAR_INDEX`. For every RNA sample with one or more personalized HLA allele references, this aligns that sample's arcasHLA MHC-extracted reads against every one of its allele-specific STAR indexes (one `STAR_ALIGN` job per `(RNA sample, allele)` pair, reusing the same reads across each of that sample's alleles). `HLAPM_RESOLVE_SAMPLE_ALLELES` bridges `sample_alleles.csv`'s WGS-individual-ID/`RNA_ONLY:<rna_id>` keying back to real RNA sample ids using `--sample_key` (broadcasting a WGS individual's full allele set to every RNA sample matched to it), publishing the resolved mapping as `hlapm/star_align/rna_sample_alleles.csv`. Alignment output (`STAR --outSAMtype BAM SortedByCoordinate`) is written under `hlapm/star_align/<rna_id>/<allele_key>/`. Continues the same STAR container/Conda exception as `STAR_GENOMEGENERATE`.
- Vendored the [nf-core/modules `SAMTOOLS_SORT`](https://github.com/nf-core/modules/tree/master/modules/nf-core/samtools/sort) module and added two new local modules (`HLAPM_COMBINE_GTF`, `HLAPM_QUANTIFY_READS`) plus a new `HLAPM_STAR_QUANTIFY` subworkflow, wired in immediately after `HLAPM_STAR_ALIGN`. For every `(RNA sample, allele)` pair, `SAMTOOLS_SORT` additionally queryname-sorts `STAR_ALIGN`'s coordinate-sorted BAM (`samtools sort -n`, published as `hlapm/star_align/<rna_id>/<allele_key>/<rna_id>.<allele_key>.queryname.bam`, alongside the unaffected coordinate-sorted BAM). `HLAPM_COMBINE_GTF` concatenates every distinct allele's GTF into one cohort-wide `hlapm/quantify/combined.gtf` (comment lines stripped), built once per pipeline run. `HLAPM_QUANTIFY_READS` then runs the legacy, unmodified Python 2.7 `make_a_table_210804_allHLAgenes.py` script (added to `bin/`) once per RNA sample, over that sample's full set of queryname-sorted per-allele BAMs plus `combined.gtf`, producing `hlapm/quantify/<rna_id>/<rna_id>.edit_distance.tsv` (per-read edit-distance/gene-assignment table) and `.stat.txt` (run statistics), run inside a new, dedicated, operator-prepared `hlapm-quantify` Conda environment (Python 2 + `pybam` + `intervaltree`; R packages for gene-level summarization are deliberately not added yet). **This is a partial quantification step**: it stops at the per-read edit-distance table and does not yet produce a gene-level read-count summary (`<rna_id>.HLA_gene_summary.tsv`) - that requires an additional R summarization step, deferred to a later iteration.
- Installed the [nf-core/modules `HLALA_TYPING`](https://github.com/nf-core/modules/tree/master/modules/nf-core/hlala/typing) module (hla-la 1.0.4, with its own `environment.yml`/`conda` directive and matching pinned Biocontainers/Galaxy-depot `container`) and rewired the `HLALA` subworkflow onto it. The module is installed unmodified. Its companion `hlala/preparegraph` module is deliberately **not** installed: the prepared HLA-LA graph stays an operator-prepared external input (`--hlala_graph_dir`/`--hlala_graph`, unchanged in name, default, and meaning), which the pipeline never builds.
- Added a new `bin/summarize_hla_readcounts.R` script (adapted from `davenportlab/HLApm_farm_pipeline`'s `04_04_24_summarize_readcounts_script.R`) and a new `HLAPM_SUMMARIZE_READCOUNTS` local module, wired into `HLAPM_STAR_QUANTIFY` immediately after `HLAPM_QUANTIFY_READS`. For every RNA sample, it turns that sample's `<rna_id>.edit_distance.tsv` per-read table into a per-gene read count, keeping only non-`ambiguous` reads with a winning-gene edit distance at or below a new `--hlapm_quantify_max_edit_distance` parameter (default `16`), and writing `hlapm/quantify/<rna_id>/<rna_id>.HLA_gene_summary.tsv` (columns `gene_name`, `n_reads_mapping`). This closes the "stops at the per-read table" gap left open by the previous bullet. Runs inside the same `hlapm-quantify` Conda environment, which now also needs R (>= 4.0) plus the `dplyr`/`tidyr` CRAN packages.
- Added `scripts/build_reference_hlala.sh <output-dir>`, an operator tool (no pipeline behaviour change) that prepares the `--hlala_graph_dir`/`--hlala_graph` input in one command: it downloads the published `PRG_MHC_GRCh38_withIMGT` PRG graph package (~2.25 GB, resumable via `curl -C -`, following the `www.well.ox.ac.uk` &rarr; `www.chg.ox.ac.uk` redirect), verifies its md5 (`525a8aa0c7f357bf29fe2c75ef1d477d`, documented in HLA-LA's README), extracts it, and indexes it with `/usr/local/opt/hla-la/bin/HLA-LA --action prepareGraph --PRG_graph_dir <graph>` inside the same pinned `hla-la:1.0.4` container image `HLALA_TYPING` itself runs (writable bind mount, since `prepareGraph` writes `serializedGRAPH` into the graph directory) - mirroring the deliberately-not-installed nf-core `hlala/preparegraph` module. Unlike `ARCASHLA_GENOTYPE`'s reference, no companion image-build script is needed: the Biocontainers/Galaxy-depot image is public. The script checks free space (~35 GB needed for the ~29 GB indexed graph plus tarball) and warns below 40 GB of RAM _before_ downloading, and is idempotent - an already-indexed graph short-circuits ahead of the download, and an interrupted run resumes at indexing. Rebuilding means deleting the graph directory; there is deliberately no force-rebuild flag. `GRAPH_NAME`/`GRAPH_URL`/`GRAPH_MD5`/`TARBALL`/`IMAGE_TAG`/`SIF_PATH`/`REQUIRED_GB` overrides support the other _published_ graph packages, offline copies, and host moves. The script (and `docs/usage.md`) state explicitly that it fetches and indexes published graphs only: constructing a PRG graph from scratch is not scriptable outside the tool author's environment (HLA-LA's `src/Update graphs.txt` documents Perl scripts on a Windows laptop, the separate older MHC-PRG v1 binary, hardcoded cluster paths, and undistributed inputs), so it is an upstream request rather than a missing feature here.
- Added an alternative genotype-side HLA caller: **HIBAG** HLA imputation from SNP-array data, as an alternative to HLA-LA from WGS. A new `--array_samples` samplesheet (`array_sample_id,array_bed_path,array_bim_path,array_fam_path`, validated by `assets/schema_array_samples.json`) supplies PLINK BED/BIM/FAM datasets; a new `bin/hibag_predict.R` script plus `HIBAG_PREDICT`/`HIBAG_COMBINE` local modules and a `HIBAG` subworkflow run `HIBAG::hlaPredict()` over every locus in a pre-fit model and write `hibag/HIBAG_combined.tsv` in **exactly** the `sample_id`/`Locus`/`HLA_allele` contract `HLALA_COMBINE` produces, so `HLA_CONSENSUS` consumes either caller's output unchanged (HLA-LA's G-groups and HIBAG's two-field alleles both collapse to the same resolution under `--hla_consensus_truncate_fields`). `--array_samples` and `--wgs_samples` are **mutually exclusive** and rejected together by a new `genotypeSourceExclusiveError()` check. New parameters: `--hibag_model` (required with `--array_samples`, checked by `hibagModelExistsError()`), `--hibag_match_type` (default `RefSNP+Position`), `--hibag_assembly` (default `hg19`), `--hibag_loci` (default: every locus in the model), and `--hibag_min_prob` (default `0`, matching HLA-LA's lack of a confidence filter). Note that a samplesheet row is one PLINK **dataset**, not one sample: HIBAG predicts every sample in a fileset at once, and the sample IDs used downstream come from the `.fam` IID column rather than from `array_sample_id`. `bin/hibag_predict.R` pre-checks model/genotype SNP overlap and, instead of HLA-LA-style abort, reports how many model SNPs each match criterion would find and which one to switch to - the common failure being a model whose coordinates are offset from the array manifest. `HIBAG_PREDICT` provisions HIBAG itself, via a module-owned `environment.yml`/`conda` directive and a matching pinned `container` directive (see `Changed` below). Added a `test_hibag` profile (`conf/test_hibag.config`), pipeline tests in `tests/hibag.nf.test`, module tests for `HIBAG_PREDICT`/`HIBAG_COMBINE`, and `HLA_CONSENSUS` tests asserting that HLA-LA and HIBAG inputs yield identical consensus output. Test fixtures under `tests/fixtures/hibag/` include HIBAG's bundled HLA-A example model (vendored, GPL-3, provenance recorded in that directory's `README.md`) and NA12878's own OmniExpress genotypes subset to that model's SNPs, making the module test a real imputation with a known right answer (`A*01:01`/`A*11:01`).
- Added `testdata-make/10-prepare-na12878-array-hibag` and `testdata-make/lib/finalreport_to_plink.py`, which download the NA12878 / GM12878 Illumina HumanOmniExpress-24 v1.0 SNP-array data from GEO (GSE96790, `GSM2544145`) and convert it into the PLINK BED/BIM/FAM genotypes HIBAG consumes. The GEO Nexus FinalReport carries no genotype calls (no `GType`/`Allele*` columns), so genotypes are derived from the B Allele Freq clusters and the `[A/B]` allele pair, with a GC Score cutoff; the conversion log records this explicitly along with per-sample call/missingness tallies and the strand-ambiguous SNP count. Output is subset to the xMHC by default (`ARRAY_REGION`, a deliberate superset of HIBAG's own import window) to keep the artifact small. The IDATs are downloaded for provenance only - genotyping them would need a licensed BPM/EGT manifest and Illumina's `iaap-cli`.
- Added `testdata-make/11-download-hibag-model`, which downloads a published HIBAG pre-fit model covering all seven classical loci (A, B, C, DRB1, DQA1, DQB1, DPB1) from the "HLARES" parameter estimates of Zheng et al. (2014), validates it structurally, and prints its locus/SNP/allele table. `HIBAG_ANCESTRY` (European/Asian/Hispanic/African) and `HIBAG_MODEL_ASSEMBLY` (hg19/hg18) select which; European/hg19 is the default, matching NA12878's CEU ancestry. The model bundled with the HIBAG R package - this repo's test fixture - covers HLA-A only, so a run using it reported one locus. Upstream publishes no checksum and no licence for these files, so the recorded md5 is a change-detector rather than an authenticity check (a mismatch warns, it does not fail), and the model is downloaded into gitignored test data rather than vendored. `pipeline_testdata_run.sh` now defaults to this model and hard-fails with instructions if it is absent, rather than silently falling back to the single-locus fixture.
- Added a `--hibag_min_matched_snps` parameter (default `0.5`) guarding against silently wrong HIBAG calls. HIBAG does **not** fail when only part of a model's SNPs are found - it returns confident-looking alleles computed from whatever matched. Observed against the published model, whose 2012-era rsIDs have largely been merged or retired: under the pipeline's default `--hibag_match_type RefSNP+Position` only 13 of 273 HLA-A SNPs matched and the run produced a spurious homozygous `C*07:01`/`C*07:01`, where `Pos+Allele` matched 271 of 273 and gave the correct `C*01:02`/`C*07:01`. `bin/hibag_predict.R` now fails below the threshold with the same diagnostic table as the zero-overlap error, naming the criterion that would work, and logs the matched fraction per locus. Set to `0` to disable.
- Vendored the [nf-core/modules `SUBREAD_FEATURECOUNTS`](https://github.com/nf-core/modules/tree/master/modules/nf-core/subread/featurecounts) module (pinned to Subread 2.1.1) and added a new `COUNTS_COMMONREF` subworkflow, wired in whenever `--rna_samples` is provided, independent of `--sample_key`/HLApm. For every RNA sample, this runs featureCounts (`--countReadPairs -g gene_id`) directly against that sample's original, full whole-genome, coordinate-sorted BAM (`--rna_samples`'s `bam`/`bai` columns) and a new, now-mandatory `--gtf` whole-genome reference GTF parameter, producing `counts_commonref/<rna_id>/<rna_id>.featureCounts.tsv[.summary]`. This is iteration 1 of 2 toward the "hijack original count matrix" roadmap item: it stops at producing the whole-genome per-sample gene-count table; reconciling it against `HLAPM_STAR_QUANTIFY`'s HLA-specific counts is deferred to a later iteration. `--gtf` is enforced via a new `gtfExistsError()` hard-error check mirroring the existing `hlapmRepoExistsError()` precedent. Added a new `--rnaseq_strandedness` parameter (default `reverse`; allowed values `unstranded`/`forward`/`reverse`), merged pipeline-wide (not per-sample) into each RNA sample's metadata before the featureCounts call. Like `STAR_GENOMEGENERATE`/`STAR_ALIGN`, Subread/featureCounts is not expected from an operator-prepared Conda environment; the pipeline relies on the module's own published container/`environment.yml` under `-profile singularity`/`docker`/`conda`.
- Added a new `bam` output to `ARCASHLA_EXTRACT` (`modules/local/arcashla/extract/main.nf`), exposing its existing, previously-undeclared `<rna_id>.mhc.namesort.bam` intermediate (the HLA-region-restricted, primary-alignment-only, name-sorted BAM slice that its own FASTQ outputs are derived from), and a new `hla_region_bam` emit on the `ARCASHLA` subworkflow forwarding it up to `workflows/hlarnaseq.nf`. Published as a side effect under `arcashla/extracted/<rna_id>.mhc.namesort.bam`.
- Added a new `COUNTS_COMMONREF_HLA` subworkflow (iteration 2 of 2 toward "hijack original count matrix"), wired in immediately after `ARCASHLA` whenever `--rna_samples` is provided, independent of `--sample_key`/HLApm. For every RNA sample, this runs `SUBREAD_FEATURECOUNTS` a second time (aliased `SUBREAD_FEATURECOUNTS_HLA` - this repository's first use of Nextflow's `include { X as Y }` module-aliasing syntax) with `-R BAM --countReadPairs -g gene_id` against `ARCASHLA_EXTRACT`'s HLA-region BAM (above) and the same cohort-wide `--gtf` reference `COUNTS_COMMONREF` already uses (no separate HLA-only GTF). A new local `COUNTS_COMMONREF_HLA_REFORMAT` process (`modules/local/counts_commonref_hla/main.nf`) then converts the `-R BAM` reannotated BAM to plain text with `samtools view`, filters to `Assigned`-status lines, and reformats the result into a `read_name`, `direction`, `gene_name`, `edit_distance` per-read TSV using a new, verbatim-ported `bin/reformat_rnaseq_featurecounts.py` script, producing `counts_commonref_hla/<rna_id>/<rna_id>.rnaseq_featurecounts.tsv` (+ `.featureCounts.tsv.summary`). This is an intermediate artifact for a future reconciliation ("hijack") step, not a final HLA gene-count table; reconciling it against `HLAPM_STAR_QUANTIFY`'s `edit_distance.tsv` remains out of scope for this iteration.
- Patched `modules/nf-core/subread/featurecounts/main.nf` via `nf-core modules patch subread/featurecounts` (this repository's first use of `nf-core modules patch`) to add a new, `optional: true` `tuple val(meta), path("*.featureCounts.bam"), emit: bam` output, capturing `-R BAM`'s previously-uncaptured per-read reannotated BAM (needed by `COUNTS_COMMONREF_HLA` above; the plain, non-`-R BAM` `COUNTS_COMMONREF` call is unaffected since the output is optional).
- Added a new `bin/reconcile_hla_readcounts.py` script (an adaptation of `artifacts/scripts/compare-hla-rnaseq-readcounts.py`'s read-pair reconciliation logic), a new `HLA_READCOUNT_RECONCILE_DIFF` local module, and a new `HLA_READCOUNT_RECONCILE` subworkflow (iteration 3 of the "hijack original count matrix" roadmap item), wired in immediately after `HLAPM_STAR_QUANTIFY` inside the existing `--rna_samples && --sample_key` block. For every RNA sample that has both a `counts_commonref_hla` read-gene-assignment table (from `COUNTS_COMMONREF_HLA`, above) and a personalized-HLA `edit_distance.tsv` (from `HLAPM_STAR_QUANTIFY`, above) - joined by a plain `.join()` on `rna_id`, an inner join that silently skips samples with no personalized-HLA quantification - this classifies every personalized-HLA read pair as kept (`missing_fc`/`both_hla`/`reassigned_to_hla`) or dropped (`dropped_due_to_better_non_hla`, a better non-HLA `counts_commonref_hla` assignment), then emits `hla_readcount_reconcile/<rna_id>/<rna_id>.hla_readcount_reconcile.tsv` with columns `gene_id`, `gene_name`, `category` (`hla`/`non_hla`), `original_fc_count`, `personalized_count`, `diff`. Unlike the prototype script (whose `value_counts()`-only approach silently dropped an HLA gene whose every read pair was reassigned away), every post-filter personalized-HLA gene is guaranteed a row, reindexed to `0` where needed; a non-HLA gene only gets a row when it lost at least one read pair to HLA. Every gene name in the output (HLA and non-HLA alike, unlike `artifacts/scripts/hijack-original-featurecounts.py`'s own `HLA-`-only gene-mapping helper) is resolved to a `gene_id` via the whole-genome `--gtf`: a name absent from `--gtf` soft-fails to the literal string `NA` (warned, not fatal), while a name ambiguously mapping to more than one distinct `gene_id` hard-fails the run (an intentional asymmetry, since this table feeds a future patching step that must not silently guess a wrong `gene_id`). Reuses the existing `--hlapm_quantify_max_edit_distance` parameter (default `16`) as this step's edit-distance threshold too. This iteration produces only the per-sample diff table; merging/patching `counts_commonref`'s whole-genome table using it remains a future iteration.

### `Changed`

- Changed `HLALA_COMBINE` to emit tab-separated `hlala/HLA-LA_combined.tsv` (was comma-separated `HLA-LA_combined.csv`), adding a `Locus` column alongside `sample_id` and `HLA_allele`.
- Changed `bin/call_hla_consensus.py`'s `load_rna_wgs_key()` to read the new 2-column `--sample-key` CSV format (`rnaseq_sample_id,wgs_sample_id`), replacing the previous `--key-tsv` format (`sanger_sample_id_rnaseq`, `sanger_sample_id_wgs`, `USUBJID` columns, with `NoWgsSangerID:<USUBJID>` synthesis for present-but-empty WGS IDs). An RNA sample absent from the key is now always treated as `RNA_ONLY:<RNA_sample_ID>`.
- Changed the `HLA_CONSENSUS`→`HLAPM`→`HLAPM_STAR_INDEX`→`HLAPM_STAR_ALIGN` chain in `workflows/hlarnaseq.nf` from running only when `--rna_samples`, `--wgs_samples`, and `--sample_key` were all provided, to running whenever `--rna_samples` (and therefore `--sample_key`) are provided, independent of `--wgs_samples`. `HLALA` remains the only optional step, still gated solely on `--wgs_samples`.
- Changed `--sample_key` from an optional parameter to a required top-level input (added to `nextflow_schema.json`'s `input_output_options.required`). RNA samples absent from the key (or every RNA sample, when the key has zero data rows) continue to run through the existing `RNA_ONLY:<rna_id>` consensus fallback path.
- Changed `--hlapm_repo` from conditionally mandatory (required only when `--wgs_samples` was also provided) to unconditionally mandatory whenever `--rna_samples` is provided, via `hlapmRepoExistsError()` in `subworkflows/local/utils_nfcore_hlarnaseq_pipeline/main.nf` (still enforced solely by this custom check, not added to schema-level `required`, consistent with the existing `hlala_graph_dir` precedent).
- Added `assets/NO_WGS_HLALA_COMBINED.tsv`, a header-only (`sample_id`, `Locus`, `HLA_allele`) placeholder used as `ch_hlala_combined` whenever `--wgs_samples` is not provided, so `HLA_CONSENSUS`'s required `hlala_combined_tsv` input always receives a valid, parseable file. This is a channel-contract change: the `hlala_combined` workflow output is no longer ever empty once the mandatory chain runs (previously it was `channel.empty()` when `--wgs_samples` was absent); no current internal consumer distinguished "ran with WGS" from "ran without" by channel emptiness.
- Changed `-profile test` to exercise the full mandatory chain (`ARCASHLA`→`HLA_CONSENSUS`→`HLAPM`→`HLAPM_STAR_INDEX`→`HLAPM_STAR_ALIGN`) end-to-end under `-stub-run`, using a new header-only `tests/fixtures/sample_key_empty.csv` fixture (`--sample_key`) and a new placeholder `tests/fixtures/hlapm_repo_stub/` directory (`--hlapm_repo`, containing no real HLApm code or reference data). Added a `stub:` block to `HLAPM_BUILD_REF` that derives individual IDs from its `sample_tsvs` input and writes placeholder `out/<individual_ID>/<allele>.{fa,gtf}` files plus `versions.yml`, so `-stub-run` never shells out to `conda run -n hlapm`. Fixed `ARCASHLA_COMBINE`'s existing `stub:` block, which previously `touch`ed a 0-byte `arcasHLA_combined.csv`, to instead write a real header row plus one synthetic allele row per sample, so the now-unstubbed `HLA_CONSENSUS` can parse it.
- Changed `ARCASHLA_GENOTYPE` to provision arcasHLA itself, via a new module-owned `environment.yml`/`conda` directive (`-profile conda`) and a new module-owned `Dockerfile`/`container` directive (`-profile docker`/`singularity`/`apptainer`), replacing the dedicated, operator-prepared `arcas-hla` Conda environment used previously. Removed `envs/arcas-hla.yml`.
- Added a required `--arcashla_reference_dir` parameter and `arcashlaReferenceDirExistsError()` hard-error check (mirroring `--hlala_graph_dir`/`hlalaGraphDirExistsError()`): arcasHLA has no option to point `genotype` at an external reference at runtime, so building it automatically (a ~4GB `github.com/ANHIG/IMGTHLA` clone) proved too slow/network-fragile to do inside a container build or as an in-pipeline first-use step. `ARCASHLA_GENOTYPE` now symlinks this externally-prepared directory into place as its own `dat/ref` at the start of every task instead. Added `scripts/build_arcashla_reference.sh` to build it once, out of band, inside the same container image `scripts/build_image_arcashla.sh` builds for the pipeline itself. This script does not just run plain `arcasHLA reference`: since IMGT/HLA's Release 3.56.0, its large files (including `hla.dat`, which arcasHLA 0.6.0 expects as a plain file) are distributed as separate `.zip` downloads instead of being checked into git, so a plain clone of the current upstream default branch no longer works with this pinned arcasHLA version at all. The script instead clones IMGT/HLA itself, checks out a pinned pre-3.56.0 commit (version 3.46.0 by default, the newest version arcasHLA 0.6.0 has a built-in commit mapping for; `git-lfs` added to `environment.yml` to materialize any LFS-tracked files at that commit), and runs `arcasHLA reference --rebuild` against it - also pinning the exact HLA database version used for reproducibility. Added `tests/fixtures/arcashla_reference_stub/` and wired `--arcashla_reference_dir` into `conf/test.config`, mirroring the existing `hlapm_repo_stub` precedent.
- Changed the `container` directive on `ARCASHLA_GENOTYPE` to resolve differently per container engine (mirroring the vendored nf-core `star/align` module's pattern): a `quay.io/...`-prefixed tag for Docker (matching `nextflow.config`'s `docker.registry`/`singularity.registry = 'quay.io'` default, so a locally-built image resolves without a registry push) or a local `.sif` file path for Singularity/Apptainer (which have no access to Docker's local image store, so a bare image name would otherwise try, and fail, to pull from `quay.io` over the network). Added `scripts/build_image_arcashla.sh` to build both locally. Added `conda-forge::procps-ng` to `environment.yml` (provides `ps`): Nextflow's task wrapper runs it inside every container to collect trace metrics and fails the whole task if it's missing, which the minimal `mambaorg/micromamba` base image doesn't ship by default. Added an explicit `ENV PATH` to the `Dockerfile`: the micromamba base image only puts its environment on `PATH` via an entrypoint script that wraps whatever command `docker run` is given, but Singularity/Apptainer's `exec` (what Nextflow actually uses to run tasks) and Nextflow's own task-runner script under either engine both exec commands directly, bypassing that wrapper - without this, every tool the environment provides (`arcasHLA`, `kallisto`, `git`, `ps`, ...) was installed but unreachable under `-profile singularity`/`apptainer`.

- Changed the `HLALA` subworkflow to run HLA-LA via the nf-core `HLALA_TYPING` module instead of the local `HLALA_RUN` module, which shelled out to a dedicated, operator-prepared `hla-la` Conda environment with `conda run -n hla-la`. HLA-LA is now provisioned by the module's own `conda`/`container` directives (`-profile conda`/`docker`/`singularity`/`apptainer`), and its version is reported as `hla-la: 1.0.4` through the `versions` topic (the tool has no `--version`, so the previous module could only report `HLA-LA: unknown`). Because `HLALA_TYPING` passes only `--customGraphDir <graph>` and never `--graph`, while `HLA-LA.pl` resolves its graph as `$customGraphDir . '/' . $graph`, the subworkflow now joins `--hlala_graph_dir` and `--hlala_graph` into the single leaf graph directory the module expects; the two user-facing parameters keep their existing meaning (parent directory + graph name). `hlala/HLA-LA_combined.tsv` is unchanged in format and content. Published per-sample output changes from a hand-picked set (`hlala/<sample_id>/R1_bestguess{,_G}.txt`, `hla.tar.gz`, `hlala.log`) to HLA-LA's complete per-sample directory (`hlala/<sample_id>/hla/*`, `reads_per_level.txt`, and its `extraction*.bam`/`remapped_with_a.bam*`/`R_{1,2,U}.fastq` intermediates), published as hard links - see the next entry and `docs/output.md`.
- Changed `conf/modules.config` to add a `withName: 'HLALA_TYPING'` block that (a) caps the module's `process_high` label (12 CPUs, 72 GB) at 6 CPUs and 36 GB, which the local executor would otherwise refuse outright on smaller machines (same class of fix as the `STAR_GENOMEGENERATE` cap below), and (b) publishes the whole per-sample output directory with `mode: 'link'` (hard links) instead of honouring `--publish_dir_mode`. Publishing from this process is all-or-nothing: the module declares both the enclosing per-sample directory and files nested inside it as outputs, and Nextflow (verified on 26.04.4) then offers only the directory name to `publishDir`'s `saveAs`/`pattern` and drops the nested paths, so keeping just the typing results is impossible without patching the module (deliberately left unmodified). Publishing everything by copy would duplicate HLA-LA's multi-gigabyte per-sample intermediates (`extraction*.bam`, `remapped_with_a.bam`, `R_{1,2,U}.fastq`) for every WGS sample; hard links cost no additional disk space and, unlike symlinks, survive deletion of the work directory. **This makes `--outdir` and the Nextflow work directory being on the same filesystem a hard requirement whenever `--wgs_samples` is used**: hard links cannot cross filesystems and Nextflow does not fall back to copying, so a cross-filesystem layout (common on HPC: results on shared storage, work on local scratch) aborts the run with `Failed to publish file: ... [link]`. `docs/usage.md` documents the requirement and a custom-config workaround.
- Changed `HLALA_COMBINE`'s `bestguess_files` input to stage with `stageAs: "?/*"` and to build its manifest from each file's staged relative path rather than its bare name: `HLALA_TYPING` emits `<prefix>/hla/R1_bestguess_G.txt`, identically named for every sample, so the previous flat staging would collide. `hlala/HLA-LA_combined.tsv` is byte-identical either way.
- Changed `assets/schema_wgs_samples.json` to require `WGS_BAI_path` to be named `<BAM file name>.bai` (i.e. to end in `.bam.bai`) rather than merely to end in `.bai`, with a matching `errorMessage`. HLA-LA resolves a BAM's index by appending `.bai` to the BAM path, and `HLALA_TYPING` (unlike the removed `HLALA_RUN`) does not symlink a differently-named index into place, so a `<BAM base name>.bai` index would now fail deep inside HLA-LA instead of at samplesheet validation. This is a stricter, user-facing validation change; `assets/wgs_samples.csv` already complies.
- Changed `ARCASHLA_VALIDATE_FASTQ` to provision its validator itself, via a new module-owned `environment.yml`/`conda` directive and a matching pinned `container` directive (`bioconda::biopet-validatefastq=0.1.1` / `quay.io/biocontainers/biopet-validatefastq:0.1.1--hdfd78af_3`, with the Galaxy-depot `.sif` URL for Singularity/Apptainer), replacing the previous expectation that a `validatefastq` executable was already on `PATH` in the operator-prepared `nf-core` environment. Unlike `ARCASHLA_GENOTYPE`, no module-local `Dockerfile` and no committed `.sif` are needed: a maintained public Biocontainer exists for this tool, so both branches of the `container` directive are public references. The invoked command changes from `validatefastq` to `biopet-validatefastq` - the Bioconda package and its Biocontainer install the tool under that name only, with no plain `validatefastq` alias - and the `versions.yml` key follows suit (`biopet-validatefastq: 0.1.1`, same value as before). Validation semantics are unchanged: a non-zero exit status _or_ a log line beginning with `ERROR` still fails the task (the tool reports pairing errors while exiting 0, so the `ERROR` grep remains the load-bearing check), and the published `arcashla/validation/<rna_id>.validatefastq.log` path and format are unchanged. Removed the now-obsolete "install validatefastq separately" note and openjdk rationale from `envs/nf-core.yml`; the module's own environment pulls in the Java runtime. There is no nf-core/modules component for this tool (verified against the full upstream module list), so the local module is kept.
- Added a `stub:` block to `ARCASHLA_VALIDATE_FASTQ`, which previously had none and therefore really invoked the validator even under `-stub-run`. It writes a synthetic log in the tool's own log4j format (rather than a bare `touch`), so `-profile test -stub-run` now needs neither the tool nor a container image while keeping `tests/default.nf.test`'s existing "no errors found" assertion meaningful. Changed the `reject mismatched extracted FASTQ pair` test - the one test that runs the validator for real - to load a new `tests/singularity.config` (conf/test.config plus Singularity enabled), so it exercises the new container path instead of a host-installed executable. A per-test `config` is used rather than a per-test profile because nf-test 0.9.5 selects profiles globally and has no test-level `profile` directive. Sibling processes that do not yet declare a `container` directive (e.g. `ARCASHLA_EXTRACT`) continue to run on the host under that profile, so this needed no other module changes.
- Changed `HIBAG_PREDICT` to provision the `HIBAG` R package itself, via a new module-owned `environment.yml`/`conda` directive and a matching pinned `container` directive (`bioconda::bioconductor-hibag=1.42.0` / `quay.io/biocontainers/bioconductor-hibag:1.42.0--r44he5774e6_1`, with the Galaxy-depot `.sif` URL for Singularity/Apptainer), following the `ARCASHLA_VALIDATE_FASTQ` precedent. `bioconductor-hibag` was correspondingly **removed** from `envs/nf-core.yml`: the SNP-array path no longer depends on HIBAG being installed in the environment Nextflow is launched from, and now requires one of `-profile conda`/`docker`/`singularity`/`apptainer`. Running with none of them fails at this step with a message naming those profiles, rather than silently imputing with whatever version is on the host `PATH`. `bin/hibag_predict.R` needs only `library(HIBAG)`, so a single-package Biocontainer suffices - no mulled image and no module-local `Dockerfile` as `ARCASHLA_GENOTYPE` needs. The pin is 1.42.0 rather than the newer 1.46.0 because 1.42.0 is the most recent release the Galaxy depot mirrors as a Singularity image (`bioconductor-hibag:1.46.0--r45ha27e39d_0` is absent there), so one version covers all four profiles. The two `HIBAG_PREDICT` module tests now load per-test configs that enable Singularity - previously, with `nf-test.config`'s global `profile "test"` enabling no container engine, they resolved HIBAG from the host `PATH` and so could not have caught this. Pipeline-level `tests/hibag.nf.test` is unaffected: it runs `-stub-run` and never invokes R.
- Changed `ARCASHLA_EXTRACT` to provision `samtools` itself, via a new module-owned `environment.yml`/`conda` directive and a matching pinned `container` directive, replacing the previous expectation that `samtools` was already on `PATH` in the operator-prepared `nf-core` environment. This was the last module in the pipeline calling a tool off the host `PATH` with no `conda`/`container` directive of its own. The pin (`bioconda::samtools=1.24` + `bioconda::htslib=1.24`) and the image (`community.wave.seqera.io/library/htslib_samtools:1.24--d697cfb9dce007cd`, with the matching Seqera container-registry URL for Singularity/Apptainer) are deliberately **identical** to the already-vendored nf-core `SAMTOOLS_SORT` module - the pipeline's only other samtools call site - so the two can never drift to different samtools versions and containerized profiles pull no image the pipeline was not already pulling. Running with none of `-profile conda`/`docker`/`singularity`/`apptainer` now fails at this step with a message naming those profiles, rather than silently slicing the MHC region with whatever samtools is on the host. The extraction command line, the published `arcashla/extracted/<rna_id>.mhc_{1,2}.fq.gz` outputs, the `versions.yml` key, and the module's channel contract are all unchanged. `bioconda::samtools` is **retained** in `envs/nf-core.yml` - no pipeline process needs it any more, but the `testdata-make/` fixture-building scripts still invoke it directly.
- Added a `stub:` block to `ARCASHLA_EXTRACT`, which previously had none and therefore really ran `samtools` off the host `PATH` even under `-stub-run` - so `-profile test -stub-run` was not the tool-free test it appeared to be. It writes a minimal but valid gzipped FASTQ pair (rather than a bare `touch`, which would break `tests/default.nf.test`'s existing non-empty assertions) and reports the pinned `samtools: 1.24`. Real, non-stub coverage of this module does not disappear: the `reject mismatched extracted FASTQ pair` test already runs it for real under `tests/singularity.config`, and now exercises it inside its container.
- Changed `scripts/build_arcashla_reference.sh`'s default `IMGTHLA_COMMIT` from `8d77b3dd93959663d58ae5b626289d0746edd0e7` (IPD-IMGT/HLA **3.46.0**) to `38398a75e9762ff070d8e9bd714d074332646cd7` (**3.52.0**, tag `v3.52.0-alpha`), so the arcasHLA reference is built from the same HLA database version HLApm uses - putting arcasHLA's genotype calls and the personalized reference HLApm builds from them on one allele nomenclature. 3.46.0 was originally chosen as the newest version arcasHLA 0.6.0 has a built-in version-to-commit mapping for (`dat/info/parameters.json`), but that mapping only backs `arcasHLA reference --version`, which this script deliberately does not use: it checks the commit out itself and runs `arcasHLA reference --rebuild`, which is what makes any pre-3.56.0 version reachable (equivalent to `arcasHLA reference --commit v3.52.0-alpha`). 3.52.0 (2023-04-17) still predates the 3.56.0 restructuring that moved large files out of git into `.zip` downloads, and its `hla.dat` is a git-lfs-tracked file the script's existing `git lfs pull` and LFS-pointer assertion already handle - so no image rebuild, no `environment.yml` change, and no change to the script's interface, output layout, or the `--arcashla_reference_dir` parameter. **Operators who already built a reference with the previous default must re-run the script to pick up 3.52.0**: the pipeline does not inspect or validate the database version of the directory passed to `--arcashla_reference_dir`. Genotype calls on real data will change (a newer allele database gives different and finer calls); `-profile test` is unaffected, as it uses the `tests/fixtures/arcashla_reference_stub/` fixture rather than a real reference.
- Changed `HLAPM_BUILD_REF` to provision its R stack itself, via a new module-owned `environment.yml`/`conda` directive and a matching module-owned `Dockerfile`/`container` directive, replacing the dedicated, operator-prepared `hlapm` Conda environment it previously invoked with `conda run -n hlapm`. Removed `envs/hlapm.yml`. Unlike the other modules moved this way, the container additionally bakes in **HLApm itself**, cloned at pinned commit `38faa6087bbd827ccab969d947f8df101e95d688` (upstream `HEAD`, 2025-06-17) into `/opt/HLApm` with its bundled `data/references/` files: HLApm is an unpackaged lab git repository with no Conda package and no releases or tags, so a commit SHA is the only reproducible handle on a version and `environment.yml` cannot supply the tool at all. Added `scripts/build_image_hlapm.sh` (mirroring `scripts/build_image_arcashla.sh`: Docker tag `quay.io/hlarnaseq/hlapm-build-ref:38faa60` plus a local `modules/local/hlapm/build_ref/hlapm-build-ref.sif`, with `HLAPM_COMMIT` overridable). The image build needs network access to `github.com`. The R package set is unchanged from `envs/hlapm.yml` and pinned to that environment's working versions (`r-base=4.5.3`, `bioconductor-biostrings=2.78.0`, `bioconductor-decipher=3.6.0`, `bioconductor-rtracklayer=1.70.1`, `r-data.table=1.18.4`, `r-dplyr=1.2.1`, `r-seqinr=4.2_44`, `r-stringr=1.6.0`, `bedtools=2.31.1`), plus `git` (used by the image build and by version reporting) and `procps-ng` (Nextflow's in-container `ps` trace call). `bin/hlapm_build_personalized_ref.R` is now invoked off `PATH` rather than as `${projectDir}/bin/...`, which is not a valid path inside a container. Personalized-reference outputs are unchanged.
- Changed `--hlapm_repo` from unconditionally mandatory (whenever `--rna_samples` and `--sample_key` are provided) to **conditionally** mandatory: required only when no container engine is in use (`-profile conda`, or no profile at all), since Conda can install HLApm's R dependencies but not a git checkout. Under `-profile docker`/`singularity`/`apptainer` it becomes an optional override of the HLApm copy baked into the image, for running a patched or newer HLApm without rebuilding it. `hlapmRepoExistsError()` in `subworkflows/local/utils_nfcore_hlarnaseq_pipeline/main.nf` now gates the "please provide" error on `!workflow.containerEngine` (null under exactly those profiles) and only checks existence when the parameter is set; the parameter itself, its schema entry, `conf/test.config`, `conf/test_hibag.config` and `tests/fixtures/hlapm_repo_stub/` are all unchanged, so every command line that worked before still works. The override reaches the container as a **staged `path` input** on `HLAPM_BUILD_REF` (built in `subworkflows/local/hlapm/main.nf`, following the `--hlala_graph_dir` precedent) rather than being interpolated into the script from `params`, so Nextflow mounts it automatically - an interpolated host path would not exist inside the container. `versions.yml`'s `HLApm` key reports the image's pinned commit (read from `/opt/HLApm/HLApm.version`) or, for an override checkout, its `git rev-parse HEAD`.
- Changed `HLAPM_QUANTIFY_READS` to provision the legacy Python 2 stack itself, via a new module-owned `environment.yml`/`conda` directive and a matching module-owned `Dockerfile`/`container` directive (`quay.io/hlarnaseq/hlapm-quantify-reads:py2.7.15` plus a local `modules/local/hlapm/quantify_reads/hlapm-quantify-reads.sif`, built by a new `scripts/build_image_hlapm_quantify.sh`), replacing the Python 2 half of the operator-prepared `hlapm-quantify` Conda environment it previously invoked with `conda run -n hlapm-quantify`. The environment is a deliberately frozen `python=2.7.15` (the newest Python 2 conda-forge ships; Python 2 has been EOL since 2020 and freezing it in an image, rather than asking operators to maintain a Python 2 environment, is the point) plus `intervaltree==3.1.0` and `pybam` **pinned by commit** `846d98603905c57c31bf7b9abaf2eb8c89899e60` - pybam has no releases, tags or PyPI package, so a commit SHA is its only reproducible handle, overridable at build time with `PYBAM_COMMIT`. `quicksect` is deliberately **not** installed: `make_a_table_210804_allHLAgenes.py` prefers it and falls back to `intervaltree`, and the fallback is the branch every result so far came from, so adding it would change code paths as a side effect of a packaging change. `bin/make_a_table_210804_allHLAgenes.py` is now invoked off `PATH` (it carries its own `#!/usr/bin/env python2` shebang) rather than as `${projectDir}/bin/...`, which is not a valid path inside a container. `versions.yml` grows from one key to three (`python2`, `intervaltree`, `pybam` - the last reporting the image's `$PYBAM_COMMIT` where available, since pybam itself reports `1.0` for every commit); the `stub:` block reports all three as `unknown`. The per-read `edit_distance.tsv`/`stat.txt` outputs are unchanged.
- Added a shared python3 + R environment and image, `containers/datatools/{environment.yml,Dockerfile,README.md}` and `scripts/build_image_datatools.sh` (`quay.io/hlarnaseq/datatools:1.0` plus a local `containers/datatools/datatools.sif`), and changed the four modules that run this repository's own `bin/` analysis scripts to use it: `HLA_CONSENSUS` (`call_hla_consensus.py`), `HLAPM_PREPARE_INPUT` (`consensus_to_hlapm.py`), `ARCASHLA_COMBINE` (`combine_arcashla_genotypes.R`) and `HLAPM_SUMMARIZE_READCOUNTS` (`summarize_hla_readcounts.R`). The first three previously had **no `conda`/`container` directives at all** and resolved `python3`/`Rscript` from whatever environment Nextflow was launched in; the fourth used `conda run -n hlapm-quantify`. Unlike every other environment file in this repository this one is deliberately **not** module-local - four near-identical copies of an overlapping package list for small scripts over the same two interpreters would drift, so all four modules point their `conda` directive at `${projectDir}/containers/datatools/environment.yml` and their `container` directive at the single image built from it (see `containers/datatools/README.md`). Package versions are pinned to those the pipeline's existing published results were produced with, with one unavoidable exception: `r-tidyr` had no build for the pinned `r-base=4.5.3` at its previous `1.2.1`/`r-base 4.0.3` combination, so it moves - `HLAPM_SUMMARIZE_READCOUNTS`'s output was diffed against a known-good run to confirm the summary is unchanged. Removed `envs/hlapm-quantify.yml` (both its consumers are now containerized) and removed `python`, `pandas`, `r-base`, `r-jsonlite`, `r-dplyr`, `r-tibble`, `r-stringr` and `r-purrr` from `envs/nf-core.yml`, which is now a **launcher-only** environment: no pipeline step depends on anything installed there, and `docs/environment_setup.md`'s environment table is down to that single row. This also removes the last `conda run -n` call sites from the pipeline. Running any of these four modules with none of `-profile conda`/`docker`/`singularity`/`apptainer` now fails fast with a message naming those profiles, as `ARCASHLA_EXTRACT`, `HIBAG_PREDICT` and `HLAPM_BUILD_REF` already do.
- Changed `-profile test` to also set `--gtf` to a new placeholder `tests/fixtures/placeholder.gtf` fixture (a real whole-genome GTF is not required for `-stub-run`, since `SUBREAD_FEATURECOUNTS`'s vendored `stub:` block never reads its inputs), and removed the previously unreferenced, stale `tests/fixtures/rnaseq/RNA_SAMPLE_1.featureCounts.txt` placeholder (this step generates a featureCounts file rather than consuming one as input, so it was dead weight, not a fixture precedent).
- Changed `bin/reconcile_hla_readcounts.py`'s gene-id resolution (see the `HLA_READCOUNT_RECONCILE_DIFF`/`bin/reconcile_hla_readcounts.py` bullet above) to no longer hard-fail the whole run when a `gene_name` maps to more than one distinct `gene_id` in `--gtf`. A real run against GENCODE v50 (`gencode.v50.primary_assembly.annotation.gtf.gz`) hit exactly this case: 484 gene names are ambiguous in that real, whole-genome GTF, including 4 HLA genes themselves (`HLA-H`, `HLA-L`, `HLA-V`, `HLA-DRB6`, each a near-identical/overlapping-gene-row GENCODE v50 annotation-duplication artifact on the primary assembly) - hard-failing on this made the pipeline fail on essentially any real whole-genome GTF, not just a contrived edge case. Resolution is now soft and category-aware: a `category: hla` output row ambiguous in `--gtf` resolves to the first `gene_id` encountered while scanning `--gtf` (`load_gene_name_to_ids()`, renamed from `load_gene_name_to_id()`, now returns every candidate id per name in that deterministic first-appearance order, not a Python `set`); a `category: non_hla` row instead keeps every candidate `gene_id`, semicolon-joined in that same order, so no id is silently dropped. Every gene-id resolution problem (both this newly-un-hard-failed ambiguous case and the pre-existing missing-gene-name-in-`--gtf` soft-fail, previously visible only via a batched stderr message) is now also recorded as a structured row in a new, always-produced (header-only when empty) `<rna_id>.gene_id_resolution_warnings.tsv` output (columns `gene_name`, `category`, `reason`, `gene_ids`, `resolution`, `resolved_gene_id`), published alongside the existing diff table under `hla_readcount_reconcile/<rna_id>/`. `HLA_READCOUNT_RECONCILE_DIFF` gains a new required `--warnings-output` CLI flag and a matching `gene_id_resolution_warnings` module/subworkflow/workflow emit (`hla_readcount_diff_warnings` at the `workflows/hlarnaseq.nf` level); the existing `read_count_diff`/`hla_readcount_diff` emit is unchanged in shape.
- Changed the last seven local modules that had **no `conda`/`container` directives at all** to declare both, so every one of the seventeen processes under `modules/local/` now provisions its own runtime rather than silently resolving tools from whatever `PATH` Nextflow was launched with. The seven are `HLA_READCOUNT_RECONCILE_DIFF` (`bin/reconcile_hla_readcounts.py`; python3, pandas), `COUNTS_COMMONREF_HLA_REFORMAT` (`samtools` + `bin/reformat_rnaseq_featurecounts.py`), and the five inline-shell steps `HLALA_COMBINE`, `HIBAG_COMBINE`, `HLAPM_COMBINE_GTF`, `HLAPM_LIST_STAR_TARGETS` and `HLAPM_RESOLVE_SAMPLE_ALLELES`. All seven point at the existing shared [`containers/datatools`](containers/datatools/README.md) environment/image rather than gaining module-local ones: the two script-running modules need exactly the interpreters it already pins, and the five shell-only ones need nothing from it at all (`bash`, coreutils, `awk`, `findutils`, `grep` come from the image's Debian base under a container profile and from the host under `-profile conda`, as nf-core's own pure-shell modules do) but declare it so they name a reproducible environment instead of none. The shared image accordingly grows from four consumers to eleven.
- Changed `containers/datatools/environment.yml` to also carry `bioconda::samtools=1.24` and `bioconda::htslib=1.24`, and bumped the image from `quay.io/hlarnaseq/datatools:1.0` to **`:1.1`** (in `scripts/build_image_datatools.sh` and in all eleven consumers' `container` directives, which must agree). `COUNTS_COMMONREF_HLA_REFORMAT` runs `samtools view` before `bin/reformat_rnaseq_featurecounts.py` and so needs `samtools` and `python3` in one environment - a combination with no prebuilt public image - and a fourth module-local `Dockerfile` for a single `samtools view` call was judged the worse trade. This is a deliberate, documented exception to `containers/datatools/README.md`'s "prefer adding a package here only if it serves a `bin/` script" rule, and the pin is deliberately identical to `ARCASHLA_EXTRACT`'s and the vendored `SAMTOOLS_SORT`'s, so the pipeline still never runs more than one samtools version. **Operators who built the image before this change must re-run `scripts/build_image_datatools.sh`**: `-profile docker` will not find `:1.1` in the local image store, and `-profile singularity`/`apptainer` would otherwise use a stale `.sif` with no `samtools`. `containers/datatools/Dockerfile` now also asserts at build time that `samtools` runs and that bash associative arrays, process substitution, `sha256sum` and `find` are present, so a future base-image change fails the build rather than the pipeline. No parameter, channel contract, or output changed.
- Changed RNA analysis from nominally conditional to unconditional. RNA-seq HLA quantification is what this pipeline does, and `--rna_samples`, `--sample_key`, `--hla_region` and `--outdir` were already in `nextflow_schema.json`'s `required` array (so an RNA-less run has never been launchable), which made the `if (params.rna_samples)` and `if (params.rna_samples && params.sample_key)` guards in `workflows/hlarnaseq.nf` dead code that only implied a non-RNA mode existed. Both guards are removed and their bodies (`ARCASHLA`, `COUNTS_COMMONREF`, `COUNTS_COMMONREF_HLA`, `HLA_CONSENSUS`, `HLAPM`, `HLAPM_STAR_INDEX`, `HLAPM_STAR_ALIGN`, `HLAPM_STAR_QUANTIFY`, `HLA_READCOUNT_RECONCILE`) now run unconditionally, so the `channel.empty()` pre-initialisations for every channel they fill are gone too; `ch_hibag_calls`/`ch_hibag_posterior` keep theirs, being genuinely conditional on `--array_samples`. `--gtf` and `--arcashla_reference_dir`, previously enforced by the Groovy `gtfExistsError()`/`arcashlaReferenceDirExistsError()` checks keyed off `params.rna_samples`, are now unconditional nf-schema `required` parameters in `reference_genome_options`, so omitting either fails with nf-schema's standard `Missing required parameter(s): ...` (printed to stderr) instead of a bespoke message. Note that this does not change `--help` output: nf-schema 2.5.1 does not annotate required parameters there, for these two or for the long-standing `--rna_samples`/`--sample_key`. `gtfExistsError()` is deleted entirely; `arcashlaReferenceDirExistsError()` keeps only its `hla.idx`/`hla.p.json` "is this a real built reference" content check, which JSON Schema cannot express. `hlapmRepoExistsError()` drops its `params.rna_samples && params.sample_key` terms and now depends only on `!workflow.containerEngine`, and the `--sample_key` channel in `PIPELINE_INITIALISATION` is built unconditionally. **No behaviour change for any run that was previously launchable**; the only user-visible difference is the wording of the failure for a run missing `--gtf` or `--arcashla_reference_dir`, and that under the unsupported `--validate_params false` those two are no longer checked at all. `--wgs_samples`, `--array_samples` (+ `--hibag_model`, `--hlala_graph_dir`), `--rna_excluded_samples`, `--wgs_excluded_samples` and (under a container profile) `--hlapm_repo` all stay optional. `conf/test_hibag.config` gains the `--gtf` fixture it was missing (that profile did not launch before), and `conf/test_full.config` is gutted to an explicit placeholder that says plainly no full-size dataset is configured yet, rather than looking like a working profile - the file, its `test_full` profile entry and `.github/workflows/awsfulltest.yml` are all kept so nf-core lint's `files_exist`/`actions_awsfulltest` checks stay valid.

### `Fixed`

- Fixed `bin/combine_arcashla_genotypes.R` returning an under-shaped, sample-ID-less row for samples whose arcasHLA genotype JSON is the valid-but-empty `{}` (not enough reads to genotype), so the combined CSV now correctly NA-pads alleles while preserving `rna_sample_id` for that sample.
- Fixed `bin/hlapm_build_personalized_ref.R` failing with `object 'DIR_personalizedHLA' not found`: HLApm's own `scripts/load_ref.R` requires this global variable to be set by its caller (per HLApm's own docs) before being sourced, but the script never set it.
- Fixed `HLAPM_LIST_STAR_TARGETS` finding zero alleles (both `hlapm/star_index_targets/*.csv` manifests ending up empty) whenever run against a real `HLAPM_BUILD_REF` output: HLApm's `bulkRNA_build_personalized_HLA_ref()` nests every individual's allele files one level deeper than the output directory it is given (under an extra `out/` it creates itself), so the previous fixed-depth directory scan never found the real per-individual directories. Discovery now recursively scans for `*.fa` files at any depth instead of assuming a fixed nesting level.
- Fixed `HLALA_COMBINE` failing outright (`Missing output file(s) 'HLA-LA_combined.tsv'`) on any run with **more than one** WGS sample. Its sample manifest was interpolated into a shell heredoc, but Nextflow strips only the _common_ leading indentation from a script block: a multi-line interpolated value drops that common indent to zero, which leaves both the heredoc's first line and its `EOF` terminator indented, so the terminator never matched, the heredoc swallowed the rest of the script, and the first row's `sample_id` gained a 4-space prefix. The manifest is now written with one indentation-proof `printf` per row. Pre-existing bug, unrelated to the `HLALA_TYPING` swap (reproduced against the previous code); single-sample output is byte-identical before and after.
- Fixed `scripts/build_arcashla_reference.sh` failing immediately under Singularity/Apptainer with `fatal: could not create work tree dir '/opt/conda/share/arcas-hla-0.6.0-2/dat/IMGTHLA': Read-only file system`. The script's Singularity branch bind-mounted only the output directory, but the build writes into the arcasHLA install tree itself (`dat/IMGTHLA` for the pinned IMGT/HLA clone, `dat/ref` for the built reference) - and a `.sif` is a read-only SquashFS at exec time for every user, root included, with none of the writable layer Docker gives a container by default. (Not a UID/ownership problem, and not fixable by `--writable-tmpfs`: that overlay is small and RAM-backed, and the ~4 GB IMGT/HLA checkout exhausts it, which is why `ARCASHLA_GENOTYPE`'s much smaller writes can use it and this cannot.) The branch now seeds a writable host directory from the image's own `dat/` (1.2 MB, mostly `dat/info`) and bind-mounts it over `dat/`, so the build writes to the host filesystem. The scratch directory defaults to a temporary `<output-dir>.build.XXXXXX` sibling removed on exit - deliberately not `$TMPDIR`, routinely a small tmpfs on HPC - and is overridable with `DAT_WORK_DIR`. Container-internal mount points are now unique per run (Singularity mounts the host `/tmp` by default, so the previous fixed `/tmp/arcashla-reference-out` was a shared host path two users would collide on). Added a `REQUIRED_GB` free-space preflight (default 15, Singularity/Apptainer only - under Docker the bulk lands in Docker's own storage) so a too-small filesystem fails before the multi-GB clone rather than partway through it, and a `RUNTIME` override (`docker`/`singularity`/`apptainer`) forcing the runtime instead of auto-detection - without which the Singularity branch cannot be exercised at all on a machine that also has the Docker image loaded, which is how this survived unnoticed. The Docker path, and the reference the script produces, are unchanged.
- Fixed every WGS sample but (at most) one failing on any multi-sample `--wgs_samples` run against a graph whose extended reference genome had not been bwa-indexed, reported by a pipeline user. HLA-LA's published `PRG_MHC_GRCh38_withIMGT` package ships `mapping_PRGonly/referenceGenome.fa` pre-indexed but `extendedReferenceGenome/` with the FASTA alone, and `HLA-LA --action prepareGraph` does not build the missing index either. HLA-LA copes by indexing it lazily instead: in its `--action HLA` path, `BWAmapper::map()` calls `make_sure_ref_is_indexed()`, which shells out to `bwa index` and writes the result next to the FASTA - back into the graph directory (`src/mapper/bwa/BWAmapper.cpp`). Since `HLALA_TYPING` receives the graph as a single shared `path` input, every per-sample task saw the index missing, all launched the same `bwa index` against the same paths, and they overwrote each other's partial output. `scripts/build_reference_hlala.sh` now builds that index itself, once, out of band, with `bwa index` inside the same pinned `hla-la:1.0.4` image it already uses for `prepareGraph` (which ships `bwa` as a Conda dependency of `hla-la`, so no new tool, environment, or image is involved), resolving the FASTA path exactly as HLA-LA does (`extendedReferenceGenomePath.txt` if present, else the conventional in-graph location) and treating HLA-LA's own `.sa`/`.ann`/`.bwt` test as the definition of "indexed". The two indexing steps are now skipped independently when their outputs are already present, so **a graph prepared by an earlier version of the script is repaired by re-running the script on the same directory** - it adds only the missing bwa index, with no re-download and no second multi-hour `prepareGraph`; the script's "already indexed, nothing to do" short-circuit correspondingly now requires both. `hlalaGraphDirExistsError()` additionally refuses to start a `--wgs_samples` run against a graph missing this index, naming the script to re-run, so the failure surfaces as one line before any task launches rather than hours in; graphs using `extendedReferenceGenomePath.txt` and stub graph directories with no extended reference FASTA are exempt from that check. No pipeline parameter, channel contract, module, or output changed.
- Fixed `ARCASHLA_GENOTYPE` failing under `-profile singularity`/`apptainer`/`docker` with arcasHLA's `FileNotFoundError: [Errno 2] No such file or directory: '.../share/arcas-hla-0.6.0-2/scripts/../dat/ref/hla.p.json'` whenever `--arcashla_reference_dir` pointed anywhere outside a path the container engine already happened to mount - most easily hit when the reference lives behind a host symlink onto a separate filesystem. `params.arcashla_reference_dir` was interpolated into the task script as a bare string, which is invisible to Nextflow's bind-mount machinery (`singularity.autoMounts`, Docker volume mapping), so the module's `dat/ref` symlink swap silently produced a dangling symlink and arcasHLA reported the missing file rather than the missing mount. The reference is now a staged process input (`path(arcashla_reference, stageAs: 'arcashla_reference')`, passed down from `workflows/hlarnaseq.nf` through the `ARCASHLA` subworkflow as a value channel), so Nextflow resolves its real path and binds it itself - the same reason `HLALA_TYPING`'s `path(graph)` input was never affected by this. The reference may now live on any filesystem the executor can read, with no `SINGULARITY_BIND`, `singularity.runOptions`, or per-process `containerOptions` bind of the operator's own. The module additionally asserts up front that the staged reference resolves and contains `hla.idx`/`hla.p.json`, so a future staging or mount regression reports itself instead of reappearing as an arcasHLA `FileNotFoundError`. No change to the module's `environment.yml`, `Dockerfile`, or container image (no rebuild needed), no parameter renamed, and no output changed; existing `-resume` work directories re-run `ARCASHLA_GENOTYPE` once, since the added input changes the task hash.
- Fixed `arcashlaReferenceDirExistsError()` accepting an empty or half-built `--arcashla_reference_dir`. `scripts/build_arcashla_reference.sh` creates its output directory before doing any work, so a build that aborted part-way (no network, too little scratch space) leaves behind a directory that passed both the previous existence check and `nextflow_schema.json`'s `exists: true`, deferring the failure to a mid-run arcasHLA `FileNotFoundError` on `dat/ref/hla.p.json`. The check now also requires `hla.idx` and `hla.p.json` - files only ever produced by `arcasHLA reference` itself - and names `scripts/build_arcashla_reference.sh` in the error. **This rejects at launch some directories that were previously accepted and then failed mid-run**, which is the intent. Stub runs are exempt, so `-profile test`'s deliberate `tests/fixtures/arcashla_reference_stub/` placeholder still works.
- Fixed `STAR_GENOMEGENERATE` requesting the `process_high` label's defaults (12 CPUs, 72 GB) regardless of genome size, which exceeded the CPUs/memory available on smaller machines. HLA allele references are a few KB each, so `conf/modules.config` now caps it at 1 CPU and 2 GB (`* task.attempt` on retry) for this process specifically.
- Fixed `SUBREAD_FEATURECOUNTS`'s version-reporting `eval` (`featureCounts -v 2>&1 | sed 's/featureCounts v//'`) corrupting the entire `pipeline_info/nf_core_hlarnaseq_software_versions.yml` file whenever `featureCounts` is not on `PATH` (i.e. without `-profile conda`/`singularity`/`docker`): the resulting `bash: ... command not found` stderr text was piped through unmodified (since it does not match `sed`'s substitution pattern) and written as the "version" string, and its embedded colon breaks YAML parsing of the whole file, not just this one entry. Bundled into the same `nf-core modules patch subread/featurecounts` operation as the new `bam` output above; the `sed` invocation now uses `-n '...p'` so only lines actually matching `featureCounts v...` are emitted, degrading gracefully to an empty/`null` version field (matching the pre-existing `STAR_ALIGN`/`STAR_GENOMEGENERATE` `eval` precedent) instead of corrupting the file. Confirmed via a real `-stub-run` in a `featureCounts`-less, container/conda-less environment that `pipeline_info/nf_core_hlarnaseq_software_versions.yml` now parses as valid YAML.

### `Dependencies`

### `Deprecated`

- Removed the generic `--input` FASTQ samplesheet contract in favor of the RNA-specific manifest.
- Removed the local `HLALA_RUN` module (`modules/local/hlala/run/`), superseded entirely by the nf-core `HLALA_TYPING` module. `modules/local/hlala/combine/` is kept - it is pipeline-specific glue with no upstream equivalent.
- Removed `envs/hla-la.yml` and the `hla-la` row from `docs/environment_setup.md`'s environment table: nothing invokes `conda run -n hla-la` any more, leaving 3 operator-prepared Conda environments (`nf-core`, `hlapm`, `hlapm-quantify`).
- Removed two previously published per-sample HLA-LA outputs: `hlala/<sample_id>/hla.tar.gz` (the `hla/` directory is now published unarchived, as part of the full per-sample directory) and `hlala/<sample_id>/hlala.log` (HLA-LA's stdout is no longer redirected to a file; it is captured in the task's `.command.out` under the work directory). `hlala/<sample_id>/R1_bestguess_G.txt` and `R1_bestguess.txt` move down one level to `hlala/<sample_id>/hla/`. `hlala/HLA-LA_combined.tsv` is unaffected.
